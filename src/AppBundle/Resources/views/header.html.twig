<header class="container-fluid">
    <div class="row">
        <img class="col-md-1 col-xs-4 img-circle" src="{{asset('image/WHATWG_logo.png')}}" title="logo" alt="logo"/>
        <h1 class="col-md-4 col-md-offset-4">BomberMan</h1>

        {% if not app.request.session.get('isConnected')%}

            {{render(controller('AppBundle:User:login'))}}
        {%else%}
            <div class="well col-md-3">
                <span >Bonjour {{ app.request.session.get('login') }} !</span><br>
                <a  href="#">Déconnexion</a>
            </div>
        {%endif%}

    </div>
    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="index.php">BOMBER</a>
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    <li class="active"><a href="#">Accueil <span class="glyphicon glyphicon-home alert-success" aria-hidden="true"></span> <span class="sr-only">(current)</span></a></li>
                    <li><a href="{#{{path('lobby')}}#}">Lobby <span class="glyphicon glyphicon-piggy-bank alert-warning" aria-hidden="true"></span></a></li>
                    <li><a href="#">jeu <span class="glyphicon glyphicon-fire alert-danger" aria-hidden="true"></span></a></li>
                    <li><a href="#">Contacts <span class="glyphicon glyphicon-earphone alert-info" aria-hidden="true"></span></a></li>
                </ul>
            </div>
        </div>
    </nav>

</header>
